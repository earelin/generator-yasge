<% if (cloudFeatures.includes('Spring Cloud Netflix Eureka')) -%>
eureka:
  client:
    enabled: false
<% } -%>
<% if (cloudFeatures.includes('Spring Cloud Config')) -%>
spring:
  cloud:
    config:
      enabled: false
<% } -%>
